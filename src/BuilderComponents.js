/*import {BuilderComponent} from "@builder.io/react";

export default () => (
    <BuilderComponent
        name="page"
        context={{
            myFunction: () => alert('Hi!')
        }}
    />
) */